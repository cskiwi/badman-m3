{
  "name": "sync-worker",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/sync-worker/src",
  "projectType": "application",
  "tags": ["scope:worker"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "command": "webpack-cli build",
        "args": ["--node-env=production"]
      },
      "configurations": {
        "development": {
          "args": ["--node-env=development"]
        }
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "sync-worker:build",
        "port": 9230,
        "inspect": "inspect",
        "host": "0.0.0.0"
      },
      "configurations": {
        "development": {
          "buildTarget": "sync-worker:build:development"
        },
        "production": {
          "buildTarget": "sync-worker:build:production"
        }
      }
    },
    "repair-encounters": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ts-node -r tsconfig-paths/register --project apps/sync-worker/tsconfig.app.json apps/sync-worker/src/repair-competition-encounter-teams.ts"
      }
    }
  }
}
